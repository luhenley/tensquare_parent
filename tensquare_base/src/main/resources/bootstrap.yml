spring:
  cloud:
    config:
      uri: http://127.0.0.1:12000  # SpringCloudConfig Server地址
      name: base    # name: 应用名称
      profile: dev # profile：环境名称  配置文件名称： name-profile.yml 或 properties
      label: master #仓库的分支名称